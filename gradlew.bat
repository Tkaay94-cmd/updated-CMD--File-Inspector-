@echo off
REM Minimal gradlew.bat placeholder — on Windows please install Gradle or run the
REM official Gradle wrapper generated with `gradle wrapper`.

SET GRADLE_VERSION=8.4
ECHO Please install Gradle or generate the official wrapper by running:
ECHO   gradle wrapper --gradle-version %GRADLE_VERSION%
EXIT /B 1
